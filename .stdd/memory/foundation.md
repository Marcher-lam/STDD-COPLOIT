# 基础约束与上下文 (Foundation)

**初始化时间:** 2026-03-26

## 核心技术栈
目前尚未检测到包管理器或代码库 (空项目)。将在第一次运行时（或正式搭建应用时）确立此处的框架、依赖和约束。

## TDD / 测试框架策略
- 将根据后续语言选择确定（例如 Jest / Vitest / Playwright / Pytest 等）。
- 强制测试先行，每一次提交必须遵循 红灯->绿灯->重构 的闭环节奏。

## 全局规范
- 采用严格的防跑偏微任务隔离（Micro-Task Isolation）。
- 遵循严密的 BDD (Given/When/Then) 业务规约。
- 绝不产生毫无必要的 Scope Creep (范围溢漏)。
